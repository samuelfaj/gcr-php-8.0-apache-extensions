# Crie e configure o buildx
docker buildx create --name mybuilder --use
docker buildx inspect mybuilder --bootstrap

# Construa a imagem especificando a plataforma
docker buildx build --platform linux/amd64 -t samuelfaj/gcr-php-8.0-apache-extensions:amd64 .

# Faça login no Docker Hub
docker login

# Envie a imagem para o Docker Hub
docker push samuelfaj/gcr-php-8.0-apache-extensions:amd64
